import type { App } from 'vue'

/**
 * Register custom directives.
 *
 * This project currently does not register any custom directives.
 */
export default function registerDirectives(_app: App) {
  return
}
